# The Forge Website

Coach PO's website — The Forge System™ | Identity coaching, Petty Little Things

## Quick Start

```bash
# Install dependencies
npm install

# Run dev server
npm run dev

# Build for production
npm run build
```

## Environment Variables

Copy `.env.local.example` to `.env.local` and fill in your values.

## Project Structure

- `/app` — Next.js app router pages
- `/components` — React components
- `/lib` — Utilities and API clients
- `/public` — Static assets

## Brand Colors

- Teal: `#008E97`
- Gold: `#C8963E`
- Dark: `#0A0A0A`
- Off-white: `#F7F4EF`
